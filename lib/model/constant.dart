class Constant {
  static const devMode = true;
  static const users = 'Users';
  static const DARKMODE = true;
}

enum ArgKey {
  user,
  downloadURL,
  filename,
  photoMemoList,
  onePhotoMemo,
  userProf,
  replies,
  comments
}
