class Constant {
  static const devMode = true;
  static const users = 'Users';
  static const DARKMODE = true;
  static const debts = 'Debts';
}

enum ArgKey {
  user,
  downloadURL,
  filename,
  debtList,
  onePhotoMemo,
  userProfile,
  replies,
  comments
}
